{
  "devCommand": "uvicorn app:server --host 0.0.0.0 --port 3000",
  "builds": [
    { "src": "*.py", "use": "@vercel/python" }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/server.py"
    }
  ]
}